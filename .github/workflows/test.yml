name: Test Setup

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r birthday_email/requirements.txt

      - name: Check secrets are configured
        env:
          MY_EMAIL: ${{ secrets.MY_EMAIL }}
          MY_PASSWORD: ${{ secrets.MY_PASSWORD }}
        run: |
          if [ -z "$MY_EMAIL" ]; then
            echo "MY_EMAIL secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "MY_EMAIL is configured"
          fi

          if [ -z "$MY_PASSWORD" ]; then
            echo "MY_PASSWORD secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "MY_PASSWORD is configured"
          fi

          echo ""
          echo "All secrets are configured!"

      - name: Check required files exist
        run: |
          echo "Checking for required files..."

          if [ -f "birthday_email/main.py" ]; then
            echo "birthday_email/main.py found"
          else
            echo "birthday_email/main.py not found!"
            exit 1
          fi

          if [ -f "birthday_email/requirements.txt" ]; then
            echo "birthday_email/requirements.txt found"
          else
            echo "requirements.txt not found!"
            exit 1
          fi

          if [ -f "birthday_email/birthdays.csv" ]; then
            echo "birthday_email/birthdays.csv found"
          else
            echo "birthday_email/birthdays.csv not found!"
            exit 1
          fi

          echo ""
          echo "All required files found!"

      - name: Test Python syntax
        run: python -m py_compile birthday_email/main.py && echo "Python syntax OK"

      - name: Success
        run: |
          echo ""
          echo "========================================"
          echo "  All checks passed!"
          echo "  Your setup is ready to go."
          echo "========================================"
